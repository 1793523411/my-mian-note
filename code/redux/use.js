import { createStore } from 'redux'

const reducer = (state, action) => {
    return new_state;
}

const store = createStore(reducer);

const action = {
    type: 'ADD_ITEM',
    payload: '<li>text</li>'
}